<?xml version="1.0" encoding="utf-8"?>
<assembly manifestVersion="1.0" xmlns="urn:schemas-microsoft-com:asm.v1">
  <assemblyIdentity version="1.0.0.0" name="VpnManager.app" />

  <trustInfo xmlns="urn:schemas-microsoft-com:asm.v2">
    <security>
      <requestedPrivileges xmlns="urn:schemas-microsoft-com:asm.v3">
        <!--
          VPN 管理器需要管理员权限，原因：
            1. Clash for Windows 的 7890 端口由服务 clash-core-service（经计划任务启动）
               托管的内核 clash-win64 提供，关闭 GUI 不会释放端口；要停止它们必须以
               管理员身份运行，否则报 Access is denied。
            2. TiziGo 的 TUN 网卡（sing-box 建立）同样涉及提权进程。
            用户级环境变量写入本身不需要管理员权限。
          没有这个清单时程序以普通用户运行，"切换到直连" 会因无法停止内核而失败。
        -->
        <requestedExecutionLevel level="requireAdministrator" uiAccess="false" />
      </requestedPrivileges>
    </security>
  </trustInfo>

  <compatibility xmlns="urn:schemas-microsoft-com:compatibility.v1">
    <application>
      <!-- Windows 10 / 11 -->
      <supportedOS Id="{8e0f7a12-bfb3-4fe8-b9a5-48fd50a15a9a}" />
    </application>
  </compatibility>
  <!--
    高 DPI 设置改由项目属性 ApplicationHighDpiMode 配置
    （WinForms 分析器 WFO0003 要求，避免与 manifest 中的声明重复）。
  -->
</assembly>
