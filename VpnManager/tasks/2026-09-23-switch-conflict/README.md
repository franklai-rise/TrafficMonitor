# Clash / TiziGo 切换冲突修复

2026-09-23 的本机操作日志显示：Clash 仍运行时先启动 TiziGo，TUN 路由已建立，但初步 HTTPS 检查失败。另一次切换中，Clash 窗口关闭后仍留在托盘及后台，原流程等待后直接中止。

本次修改为：先核实并退出旧 VPN，确认其端口或网卡释放，再启动目标并检查独立路径；若客户端仅隐藏到托盘，短暂等待后只结束安装目录内、名称已核实的界面和内核进程。失败恢复先清理新目标，再恢复原 VPN 和用户代理变量。

只运行模拟进程、路由、HTTP 与代理变量的离线检查。当前依赖 VPN 的 Codex 会话中不执行真实切换。真实双向切换和失败恢复仍待空闲时段验收。
